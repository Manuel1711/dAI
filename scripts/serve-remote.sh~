#!/bin/bash
# serve-remote.sh — avvia SOLO il server locale (niente cloudflare)

PORT=${1:-5173}
SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"

echo "[serve-remote] Avvio server HTTP su porta $PORT..."
cd "$SCRIPT_DIR"

python3 -m http.server "$PORT"
